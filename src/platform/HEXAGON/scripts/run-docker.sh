#!/bin/bash

# Run this from the betaflight project top level directory
# docker run -it --rm --privileged -v `pwd`:/usr/local/workspace voxl-docker-ardupilot-build
docker run -it --rm --privileged -v `pwd`:/usr/local/workspace rb5-flight-px4-build-docker